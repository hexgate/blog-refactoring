CREATE TABLE USER (
  ID VARCHAR PRIMARY KEY,
  NAME VARCHAR,
  VIP BOOLEAN
);

CREATE TABLE PRODUCT (
  ID VARCHAR PRIMARY KEY,
  NAME VARCHAR,
  PRICE NUMERIC
);

CREATE TABLE ORDER_POSITION (
  ID VARCHAR PRIMARY KEY,
  PRODUCT_ID VARCHAR,
  QUANTITY NUMERIC
);



